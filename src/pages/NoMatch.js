import React from 'react';

const NoMatch = () =>{
    return(
        <React.Fragment>
            <h1>The page requested is not found</h1>
        </React.Fragment>
    );
}

export default NoMatch;